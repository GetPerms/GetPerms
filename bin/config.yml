#GetPerms config

#Config version
#Do not change
cfgV_info: "Do not change"
cfgV: "1.0.7"

#Is it the first run?
info_firstRun: "Do not change. May still be true after you run it, but just leave it. Leave it. Leave it. Don't change it. Let it be."
firstRun: true

#Should this plugin send usage stats to metrics.griefcraft.com?
info_sendStats: "Should this plugin send usage stats to metrics.griefcraft.com?"
sendStats: true

#Weather or not to automatically generate the permissions files on startup
info_autoGen: "Weather or not to automatically generate the permissions files on startup"
autoGen: true

#Weather or not to check for updates
info_autoUpdate: "Weather or not to check for updates"
autoUpdate: true

#Should it automatically download the newest GetPerms.jar?
info_autoDownload: "Should it automatically download the newest GetPerms.jar?"
autoDownload: false

#Disable the plugin once it finishes?
#This will disable the command to regenerate the permission files!
info_disableOnFinish: "Disable the plugin once it finishes?"
disableOnFinish: false

#Download the latest dev build? May have bugs and errors.
info_devBuilds: "Download the latest dev build? May have bugs and errors."
devBuilds: false

#Debug if needed
info_debugMode: "Debug mode if needed for bugs/errors/info"
debugMode: false
